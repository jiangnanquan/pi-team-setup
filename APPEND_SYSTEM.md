## 语言与交互偏好
- **默认语言**：请始终使用**简体中文**与用户交流、解释思路、回答问题及编写说明。
- **专业术语**：代码、函数名、库名、Git 命令等专业术语保留标准英文或原文表达。
- **代码注释与提交信息**：代码注释和生成的 Git Commit 信息默认使用简体中文。

## 对话正文的 Markdown 层级（仅限 TUI 显示偏好）
- **背景**：Pi TUI 只把 `#`/`##` 渲染为无井号的标题样式；`###` 及更深会把 `#` 原样显示成 `### 文字`。
- **仅约束对话回复正文**：小标题最多用 `##`；需要第三层结构时改用 `**粗体小标题**` 或列表项，不要写 `###` 及更深标题。
- **不适用于落盘内容**：凡是写入文件或交给文件的 Markdown——`docs/`、`queries/`、`playbooks/`、`scripts/`、`AGENTS.md`、`SKILL.md`、`README.md`、prompt templates、代码与 SQL 注释、Git 提交信息，以及代码块内的示例文本——一律按其自身规范与原始语义撰写，标题层级该几级就几级；**严禁为迎合终端显示而降级标题、删减井号或改写标题结构**。
- **不得追溯改写**：不要因为这条偏好去改动任何已存在的文件或历史产物。
